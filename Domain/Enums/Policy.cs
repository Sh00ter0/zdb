using System;
using System.Collections.Generic;
using System.Text;

namespace Domain.Enums
{
    public class Policy
    {
        public const string TargetAccess = nameof(TargetAccess);
        public const string ZabbixIngress = nameof(ZabbixIngress);
    }
}
